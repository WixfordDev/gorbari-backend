# pnpm configuration
# Suppress the "ignored build scripts" prompt for known-safe optional deps.
# These packages are not imported anywhere in src/ (telemetry / native perf stats).
ignored-built-dependencies[]=@scarf/scarf
ignored-built-dependencies[]=event-loop-stats

# Use a strict, deterministic install by default
engine-strict=false
